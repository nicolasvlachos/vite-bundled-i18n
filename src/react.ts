/**
 * vite-bundled-i18n — React bindings.
 *
 * Provides `I18nProvider` (context + dictionary loading), `useI18n`
 * (scoped translation hook), and `DevToolbar` (dev-only overlay).
 *
 * @packageDocumentation
 */
export { I18nProvider } from './react/I18nProvider';
export { useI18n } from './react/useI18n';
export { DevToolbar } from './react/DevToolbar';
